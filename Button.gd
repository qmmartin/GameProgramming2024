extends Button

var currentNode : Node
var newNode : Node

func _ready():
	pass
	# Initialize variables with references to nodes
	#currentNode = get_node("res://scenes/Main.tscn")
	#newNode = get_node("res://scenes/Lev1.tscn")
	#
#func _on_button_pressed():
	 #Called when the button is pressed
	#if currentNode:
		#currentNode.visible = false
#
	#if newNode:
		#newNode.visible = true
